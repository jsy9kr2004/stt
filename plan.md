# 한국 주식 단타 ML 보조 시스템 — 개발 계획

## 1. 프로젝트 개요

### 정체성
- **이름(가칭)**: STT (Smart Trading Toolkit)
- **본질**: "개인 트레이딩 연구실 + 실시간 신호 보조 시스템"
- **자동매매 아님**: 매매 의사결정·실행은 사용자가 직접. 시스템은 buy/hold/sell 의견을 실시간으로 전달하는 보조 도구.

### 목적
1. 주식 초보 사용자가 데이터 기반으로 자신에게 맞는 트레이딩 스타일(스캘핑·데이트레이딩·스윙)을 발견
2. 검증된 ML 모델로 실시간 매매 신호 보조
3. 외부에서도 아이패드로 모니터링 가능한 구조

### 시스템 사용 시나리오
1. 집 PC가 백엔드 서버로 24/7 동작 (장중에는 실시간 데이터 수집·분석)
2. 사용자는 아이패드 브라우저로 대시보드 접속 (Tailscale 통해 외부 접근)
3. 워치리스트 종목에 신호 발생 → 웹푸시 알림
4. 사용자가 알림 보고 직접 증권사 앱에서 매매 실행

---

## 2. 설계 원칙 및 현실 인식

### ML의 한계 인정
- 시장은 노이즈가 많고 비정상적(non-stationary)이며 적대적
- 백테스트에서 화려한 모델 대부분은 과적합
- "ML로 무조건 돈 번다"는 환상은 버리고 시작

### 핵심 원칙
1. **연구가 먼저, 신호는 결과물**: 시스템의 본체는 백테스팅·전략 탐색 인프라
2. **모의 매매 의무화**: 실거래 전 최소 1~3개월 페이퍼 트레이딩으로 검증
3. **워크포워드 검증**: 단순 train/test split 금지 (시계열 데이터 누설 방지)
4. **현실적 비용 반영**: 수수료·세금(0.18%)·슬리피지 백테스트에 포함
5. **해석 가능성 우선**: 블랙박스 모델보다 LightGBM + SHAP으로 "왜 이런 신호인가" 설명 가능
6. **점진적 복잡도 증가**: 룰 기반 → 단순 ML → 복잡 ML 순서

---

## 3. 시스템 아키텍처

```
┌────────────────────────────────────────────────────────┐
│                    집 PC (Windows)                      │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  데이터 수집  │  │   ML 추론    │  │  백테스팅    │ │
│  │  (KIS API +  │→ │  (LightGBM)  │  │   엔진       │ │
│  │   pykrx)     │  │              │  │              │ │
│  └──────────────┘  └──────┬───────┘  └──────────────┘ │
│                           ↓                            │
│                  ┌────────────────┐                    │
│                  │  FastAPI 서버   │                    │
│                  │  + WebSocket   │                    │
│                  └────────┬───────┘                    │
└───────────────────────────┼────────────────────────────┘
                            │
                    Tailscale VPN
                            │
                ┌───────────┴────────────┐
                │       iPad (외부)       │
                │   ┌────────────────┐   │
                │   │  웹 대시보드    │   │
                │   │  (브라우저)     │   │
                │   └────────────────┘   │
                │   ┌────────────────┐   │
                │   │  웹푸시 알림    │   │
                │   └────────────────┘   │
                └────────────────────────┘
```

### 컴포넌트
- **데이터 레이어**: KIS API (실시간), pykrx (과거 데이터), DART (공시)
- **분석 레이어**: 피처 엔지니어링 → ML 모델 → 신호 생성
- **서빙 레이어**: FastAPI REST + WebSocket
- **프론트엔드**: 반응형 웹 대시보드 (iPad 최적화)
- **알림 레이어**: 브라우저 Web Push API

---

## 4. 기술 스택

### 백엔드
- **언어**: Python 3.11+
- **웹 프레임워크**: FastAPI + Uvicorn
- **비동기**: asyncio, websockets
- **데이터**: pandas, polars, numpy
- **ML**: LightGBM (메인), scikit-learn (보조)
- **기술적 지표**: pandas-ta (50+ 지표 내장)
- **백테스팅**: 자체 구현 (워크포워드 + 거래비용 반영)
- **데이터베이스**: SQLite (시작) → PostgreSQL/TimescaleDB (확장 시)
- **작업 큐**: APScheduler (스케줄링), 필요 시 Celery

### 프론트엔드
- **프레임워크**: React + Vite (또는 SvelteKit)
- **스타일**: TailwindCSS
- **차트**: TradingView Lightweight Charts
- **상태 관리**: Zustand 또는 React Query
- **실시간**: WebSocket 클라이언트

### 데이터 소스
- **실시간 시세**: 한국투자증권(KIS) Open API
  - WebSocket: 실시간 체결·호가 (동시 구독 41개 한도)
  - REST: 일·분봉, 잔고 조회
- **과거 데이터**: pykrx (무료, 분봉·일봉·재무)
- **공시**: DART OpenAPI (Phase 3)
- **뉴스**: 네이버 금융 크롤링 또는 RSS (Phase 3)

### 인프라
- **외부 접근**: Tailscale VPN
- **알림**: Web Push API (VAPID 키 사용)
- **로깅·모니터링**: Loguru + 자체 대시보드

---

## 5. Phase 1: 연구 인프라 구축 [현재 집중]

> 목표: 어떤 시간대·전략·종목 조합이 통하는지 데이터로 발견하는 인프라 완성

### 5.1 데이터 파이프라인
- [ ] pykrx로 거래대금 상위 종목 과거 1~3년 분봉 수집 (백테스트용 상위 200~300)
- [ ] 일봉·5분봉·30분봉 다중 시간대 저장
- [ ] SQLite 또는 Parquet로 효율적 저장
- [ ] 일일 데이터 갱신 스케줄러
- [ ] 데이터 무결성 검증 (결측·이상치)
- [ ] **Survivorship bias 회피**: 상장폐지·관리종목 history 포함

### 5.2 종목 유니버스 관리

> 한국 단타 업계 통설: **거래대금 기반 동적 선정**이 표준. 코스피 200 같은 인덱스 기반은 시총 가중이라 단타 변동성 종목이 누락됨. 코스피·코스닥 통합 운용이 일반적 (자연스럽게 코스닥 60~70%).

**유니버스 사이즈 (용도별 분리)**:
| 용도 | 종목 수 | 근거 |
|------|---------|------|
| 실시간 모니터링·추론 | **거래대금 상위 100** | KIS WebSocket 한도(41) + REST 폴링 조합, HTS·API 한도 현실적 |
| 백테스트 학습 | **거래대금 상위 200~300** | 통계적 유의성 확보 (강환국 책 다수 사례) |
| 핵심 워치리스트 | **30~50개** | Phase 1 후반에 ML 기반 2차 필터링 결과 |

**1차 필터(룰 기반, 매일 동적 재선정)**:
- 거래대금 상위 100 (실시간) / 200~300 (백테스트)
- **시가총액 500억 이상** (단타 한정 시 300억까지 허용)
- 제외 대상:
  - 관리종목·투자경고·투자위험·단기과열종목
  - VI(변동성완화장치) 발동 종목
  - 우선주·스팩(SPAC)·ETF·ETN
  - 거래정지 종목
  - 신규상장 1개월 이내
- **Point-in-time 선정**: 매일 그 시점 기준으로 재계산 (look-ahead bias 방지)

**2차 필터(ML 기반, Phase 1 후반)**:
- 종목별 백테스트 성과 측정
- "Stock-Strategy Fit Score" 계산 → 30~50개 핵심 풀로 축소

### 5.3 피처 엔지니어링
다음 카테고리에서 광범위하게 시작 (50~150개):

| 카테고리 | 예시 |
|----------|------|
| 가격 기반 | 수익률(1·5·10분), 변동성, ATR, 가격 대비 위치 |
| 추세 지표 | MA(5·20·60), MACD, ADX |
| 모멘텀 | RSI, 스토캐스틱, Williams %R |
| 변동성 | 볼린저밴드, ATR, 표준편차 |
| 거래량 | OBV, MFI, 거래량 MA 비율 |
| 호가/체결 | 체결강도, 매수·매도 호가 잔량 비율 (실시간만) |
| 시장 컨텍스트 | 코스피·코스닥 지수, 업종 지수 동기화율, **시장 구분 더미(KOSPI/KOSDAQ)** |
| 시간 | 장 시작/마감 거리, 요일, 점심시간 여부 |

### 5.4 라벨링 (Triple Barrier Method)
- 시점 t에서 매수했다고 가정
- N분(또는 N봉) 안에 다음 중 먼저 발생한 것을 라벨로:
  - 상단 배리어(+X% 도달) → **BUY**
  - 하단 배리어(-Y% 도달) → **SELL**
  - 시간 만료 → **HOLD**
- 시간대별로 다른 라벨링 파라미터 적용

### 5.5 다중 시간대 모델 학습
3가지 시간대 모델을 병렬로 학습·비교:

| 모델명 | 시간대 | 예측 호라이즌 | 트레이딩 스타일 |
|--------|--------|---------------|-----------------|
| Model-S | 1분봉 | 5~30분 | 스캘핑 |
| Model-D | 5분봉 | 1~3시간 | 데이트레이딩 |
| Model-W | 30분봉 | 1~5일 | 스윙 |

- LightGBM 멀티클래스 분류 (BUY / HOLD / SELL)
- 클래스 불균형 처리 (class_weight)
- 하이퍼파라미터 튜닝 (Optuna)

### 5.6 백테스팅 엔진
- **워크포워드 검증**: 시간 순서대로 학습→예측→재학습 반복
- **거래 비용**: 매매 수수료 + 세금(0.18%) + 슬리피지
- **현실적 체결**: 시장가 슬리피지, 호가 갭, 부분 체결
- **포지션 관리**: 최대 보유 종목 수, 종목당 최대 비중

**평가 지표**:
- 승률 (Win Rate)
- 누적/연환산 수익률
- 최대낙폭 (MDD)
- 샤프 비율, 소르티노 비율
- 평균 수익 / 평균 손실 (Profit Factor)
- 평균 보유 시간
- 거래 횟수

### 5.7 결과 분석 및 인사이트 도출
- SHAP로 피처 중요도 시각화
- 시간대별 모델 성과 비교 → "내게 맞는 스타일" 결정
- 종목별 모델 적합도 → 워치리스트 후보 선정
- 시장 국면(상승·하락·횡보)별 성과 분리 분석

### Phase 1 완료 기준
- [ ] 거래대금 상위 200~300 종목, 최소 1년 분봉 데이터 확보 (상폐 종목 포함)
- [ ] 동적 유니버스 재선정 로직 (point-in-time)
- [ ] 50+ 피처 자동 생성 파이프라인
- [ ] Triple Barrier 라벨링 구현
- [ ] 3개 시간대 모델 학습 완료
- [ ] 워크포워드 백테스트 결과 리포트
- [ ] "내게 맞는 시간대 + 종목 30~50개" 선정 완료

---

## 6. Phase 2: 실시간 신호 시스템 [예정]

> 목표: Phase 1에서 검증된 모델을 실시간으로 운영, 모의 매매로 검증

### 주요 작업
- KIS API WebSocket 연동 (워치리스트 실시간 구독)
- KIS API REST 폴링 (스캐너 모드, 5~10초 주기)
- 실시간 피처 계산 파이프라인
- 학습된 모델 추론 서빙
- FastAPI 서버 + WebSocket 푸시
- React 대시보드 (iPad 반응형)
  - 워치리스트 패널 (실시간 신호)
  - 차트 + 지표 표시
  - 호가창
  - 알림 히스토리
  - 백테스트 결과 뷰어
- Web Push 알림 (VAPID)
- Tailscale 외부 접근 설정
- **모의 매매 모드**: 신호대로 매매했다고 가정한 가상 포트폴리오 추적

### Phase 2 완료 기준
- [ ] 장중 실시간 신호 발생·표시 동작
- [ ] iPad에서 외부 접근 + 푸시 알림 수신
- [ ] 모의 매매 1~3개월 누적 → 백테스트 결과와 실제 성과 비교

---

## 7. Phase 3: 운영 및 고도화 [예정]

> 목표: 실거래 검증, 지속적 학습, 추가 피처 도입

### 주요 작업
- 실거래 데이터 누적 → 모델 재학습 파이프라인
- 모델 드리프트 감지 (성능 저하 알림)
- DART 공시 데이터 통합
- 뉴스 감성 분석 (한국어 LLM 활용)
- LLM 기반 신호 설명 (Claude로 "왜 이 신호인지" 자연어 생성)
- 멀티 전략 앙상블
- 리스크 관리 강화 (포트폴리오 레벨)

---

## 8. 마일스톤

| 마일스톤 | 목표 | 예상 기간 |
|----------|------|-----------|
| M1 | 데이터 수집 파이프라인 + 거래대금 상위 200~300 종목 1년 분봉 확보 | 1~2주 |
| M2 | 피처 엔지니어링 + Triple Barrier 라벨링 | 1~2주 |
| M3 | 3개 시간대 LightGBM 모델 학습 | 1~2주 |
| M4 | 워크포워드 백테스트 엔진 + 첫 결과 분석 | 2~3주 |
| M5 | "내게 맞는 시간대·종목" 결정 (Phase 1 완료) | - |
| M6 | KIS API 실시간 연동 + 추론 서버 | 2~3주 |
| M7 | React 대시보드 + 웹푸시 + Tailscale | 2~3주 |
| M8 | 모의 매매 모드 가동 (Phase 2 완료) | - |
| M9+ | 실거래 검증, Phase 3 고도화 | 지속 |

---

## 9. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|--------|------|------|
| 모델 과적합 | 백테스트는 좋지만 실전에서 손실 | 워크포워드 검증, 모의 매매 의무화 |
| 데이터 누설 (Look-ahead bias) | 비현실적 백테스트 결과 | 시점 t의 정보만으로 피처 생성, 라벨링 시 미래 정보 분리 |
| KIS API 장애·한도 | 실시간 신호 중단 | 폴백 (REST 폴링), 한도 모니터링 |
| 시장 국면 변화 | 학습된 패턴 무력화 | 정기 재학습, 드리프트 감지 |
| 거래 비용 과소평가 | 실전 수익률 < 백테스트 | 비용·슬리피지 보수적 추정 |
| 심리적 편향 (시스템 신호 무시) | 시스템 가치 무력화 | 신호 이력 추적, "신호대로 했다면" 비교 표시 |
| 외부 접근 보안 | 계좌 정보 노출 가능 | Tailscale (VPN), API 키 환경변수, HTTPS 필수 |

---

## 10. 다음 액션

Phase 1 진입을 위한 즉시 작업:

1. **개발 환경 셋업**
   - Python 3.11+ 가상환경
   - 주요 패키지 설치 (pykrx, lightgbm, pandas-ta, fastapi 등)
   - 프로젝트 디렉토리 구조 설계

2. **KIS API 계좌·앱 등록**
   - 한국투자증권 계좌 (없으면 개설)
   - KIS Developers 사이트에서 앱 키 발급

3. **데이터 수집 PoC**
   - pykrx로 거래대금 상위 200 종목 1주일치 5분봉 수집 테스트
   - 저장 포맷 결정 (Parquet 추천)

4. **레포지토리 구조 초안**
   ```
   stt/
   ├── data/              # 수집된 데이터 (gitignore)
   ├── src/
   │   ├── collectors/    # KIS, pykrx 데이터 수집
   │   ├── features/      # 피처 엔지니어링
   │   ├── labels/        # Triple Barrier 라벨링
   │   ├── models/        # 학습·추론
   │   ├── backtest/      # 백테스팅 엔진
   │   ├── api/           # FastAPI 서버
   │   └── notifier/      # 웹푸시
   ├── notebooks/         # 분석·실험
   ├── frontend/          # React 앱
   ├── tests/
   └── plan.md
   ```

---

## 11. 결정 보류 사항 (추후 논의)

- [ ] 프론트엔드 프레임워크 최종 결정 (React vs SvelteKit)
- [ ] 데이터베이스 선택 시점 (SQLite → PostgreSQL 전환 기준)
- [ ] LLM 활용 범위와 비용 한도
- [ ] 실거래 진입 시점 기준 (모의 매매 성과 임계값)
- [ ] 자동매매 기능 추가 여부 (현재는 명시적으로 제외)
